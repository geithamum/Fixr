$schema: ../../schema/mcp-agent.config.schema.json


anthropic:
  api_key: 

openai:
  api_key: 

ollama:
  model: llama3.2

mcp:
  servers:
    everything-search:
      env:
        EVERYTHING_SDK_PATH: "C:/Users/Geith/Documents/GitHub/Fixr/Everything64.dll"
    cli-mcp-server:
      env:
        ALLOWED_DIR: "C:/"
        ALLOWED_COMMANDS: "all"
        ALLOWED_FLAGS: "all"
        MAX_COMMAND_LENGTH: "1024"
        COMMAND_TIMEOUT: "30"
        ALLOW_SHELL_OPERATORS: "true"