$schema: ../../schema/mcp-agent.config.schema.json


anthropic:
  api_key: 

openai:
  api_key: 
ollama:
  model: llama3.2

mcp:
  servers:
    everything-search:
      env:
        EVERYTHING_SDK_PATH: "./Everything64.dll"
